# Railway deployment configuration
# This file is no longer needed - using Railway instead
      - key: PORT
        fromService:
          type: web
          property: port
      - key: USER_TIMEZONE
        value: Africa/Accra
